import React, { useEffect } from "react";
import { TreeNode } from "../interface/Tree";

interface TreeViewProps {
  tree: TreeNode[];
}
export const TreeView: React.FC = () => {
  useEffect(() => {}, []);
  return <div></div>;
};
